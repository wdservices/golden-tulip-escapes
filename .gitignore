# Remove zips from tracking
git rm --cached frontend-dist.zip backend-deploy.zip dist.zip "dist (2).zip"

# Commit the removal
git commit -m "Remove large zip files from repo"

# Rewrite history to purge all zip files
git filter-repo --path-glob '*.zip' --invert-paths

# Force push the cleaned history
git push origin --force --all